<UserControl x:Class="PleaseTweakWindows.Views.SubTweakView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Style="{StaticResource SubTweakItem}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="{Binding Name}"
                       VerticalAlignment="Center"
                       FontSize="13"
                       Foreground="{StaticResource PtwTextPrimaryBrush}"
                       TextWrapping="Wrap"
                       Margin="0,0,8,0"
                       ToolTip="{Binding Description}" />

            <Button Grid.Column="1"
                    Content="Apply"
                    Style="{StaticResource ApplyButton}"
                    Command="{Binding ApplyCommand}"
                    IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBool}}"
                    Margin="8,0,0,0" />

            <Button Grid.Column="2"
                    Content="Revert"
                    Style="{StaticResource RevertButton}"
                    Command="{Binding RevertCommand}"
                    Visibility="{Binding HasRevert, Converter={StaticResource BoolToVisibility}}"
                    IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBool}}"
                    Margin="6,0,0,0" />

            <ProgressBar Grid.Column="3"
                         Style="{StaticResource PtwSpinner}"
                         Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisibility}}"
                         Margin="8,0,0,0" />
        </Grid>
    </Border>
</UserControl>
